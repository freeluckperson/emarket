const cart = [];

const handleCart = (state = cart, action) => {
  const { type, payload } = action;
  switch (type) {
    case "ADD ITEM":
      break;

    default:
      break;
  }
};

export default handleCart;

// ADD ITEM
// DELETE ITEM
